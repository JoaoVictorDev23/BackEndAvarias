create TABLE armazem(

    armazem_id INTEGER NOT NULL PRIMARY KEY IDENTITY(1,1),
    armazem_nome VARCHAR(255) NOT NULL,
    armazem_endereco VARCHAR(255) NOT NULL,
    armazem_filial VARCHAR(255) NOT NULL

);
